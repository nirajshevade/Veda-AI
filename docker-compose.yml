services:
  veda-ai:
    build:
      context: ./web
      dockerfile: Dockerfile
    container_name: veda-ai-app
    ports:
      - "${PORT:-3001}:3000"
    environment:
      - NODE_ENV=production
    env_file:
      - ./web/.env.local
    restart: unless-stopped
